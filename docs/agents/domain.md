# Domain Docs

Before exploring, read the root `CONTEXT.md` and relevant ADRs under `docs/adr/` when they exist. Missing domain files are not errors; proceed silently.

## Layout

This is a single-context repository:

/
├── CONTEXT.md
├── docs/adr/
└── src/

Use terminology defined in `CONTEXT.md`. Surface any proposal that conflicts with an existing ADR instead of silently overriding it.
